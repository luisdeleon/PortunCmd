// MSW (Mock Service Worker) is disabled for this project
// The fake-api handlers are kept for reference but not active

export default function () {
  // MSW disabled - using real Supabase backend
}
